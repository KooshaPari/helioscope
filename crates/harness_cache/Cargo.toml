[package]
name = "harness_cache"
version = "0.2.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["sync", "rt"] }

[dev-dependencies]
criterion = "0.5"
tokio = { version = "1", features = ["full"] }

[[bench]]
name = "cache_bench"
harness = false
