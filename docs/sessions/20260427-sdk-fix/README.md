# heliosCLI SDK Fix

## Goal
Fix issue #129 by restoring `codex-rs/rmcp-client` buildability against the current `rmcp`
SDK surface.

## Scope
- Keep the change limited to `codex-rs/rmcp-client`.
- Document the upstream API mismatch and the smallest compatible patch.
- Validate the touched crate only.

## Status
Complete.
