# Implementation Strategy

The change is intentionally narrow:

- Add one Markdown badge to the existing README badge block.
- Keep the badge near the existing project-status badges for visibility.
- Record the work in session docs because the canonical checkout contains unrelated local changes.
