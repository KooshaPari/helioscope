# Plan

1. Update the `codex-rmcp-client` dependency to the `reqwest` version expected by `rmcp`.
2. Replace the `CallToolRequestParams` struct literal with the upstream constructor.
3. Run focused checks on `codex-rmcp-client` and record the exact outcome.
